import React from 'react';
import {Box, Button} from '@chakra-ui/core';

const Login = () => {
  return (
    <Box marginTop={2}>
      <Button marginRight={2}>Prev</Button>
      <Button>Next</Button>
    </Box>
  );
};

export default Login;
